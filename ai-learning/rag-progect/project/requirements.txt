# 基础Web服务
fastapi>=0.104.1
uvicorn>=0.24.0

# RAG核心
langchain>=0.1.0
langchain-community>=0.0.20
faiss-cpu>=1.7.4  # 轻量向量库（替代Milvus，先不用部署服务）
python-dotenv>=1.0.0

# 向量化+LLM（用通义千问，需提前申请API Key）
dashscope>=1.14.0  # 阿里云通义千问SDK
