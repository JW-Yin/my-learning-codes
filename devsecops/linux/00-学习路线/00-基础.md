# 《Linux 内核与云计算面试攻坚：从熟练到精通》

## 前言  
- **目标读者**：云计算/SRE 面试候选人，期望在面试中展现 Linux 深度  
- **全书主轴**：原理驱动命令，故障场景串联，优先级 P0 > P1 > P2  
- **实验约定**：全程终端操作，基于云主机或本地虚拟机，远离图形界面  

---

# 第1章 命令行肌肉记忆：文件系统与文本处理  
**本章目标**：建立终端操作安全感，精通文件与文本处理，掌握最小脚本能力辅助实验  

### 1.1 极简环境搭建与工作流  
#### 1.1.1 发行版选择与最小化安装  
- **P0** 无 GUI 的 CentOS Stream / Ubuntu Server，仅开启 SSH  
- **P0** 云主机快照与回滚策略，打造安全实验环境  
#### 1.1.2 终端复用与效率技巧  
- **P0** `tmux` 分屏、会话保持  
- **P0** 命令行补全、历史命令展开、通配符、别名（`alias`）  

### 1.2 文件系统与目录结构  
#### 1.2.1 FHS 标准目录速查  
- **P0** 各目录核心用途：`/etc`，`/var`，`/tmp` 等  
- **P0** `/proc`、`/sys`、`/dev` 浅层定位：内核与设备的交互窗口（**深入原理后置**）  
#### 1.2.2 文件属性与 inode  
- **P0** `stat` 解读：inode 编号、链接数、时间戳  
- **P0** 文件类型：普通文件、目录、符号链接、块设备、字符设备、socket  
#### 1.2.3 硬链接与软链接  
- **P0** 硬链接共享 inode，软链接是路径快捷方式  
- **P0** 面试引申：删除源文件后各自表现，`ln` 与 `ln -s` 实战  

### 1.3 文件操作命令实战  
#### 1.3.1 基本文件操作  
- **P0** `ls`, `cd`, `mkdir`, `cp`, `mv`, `rm` 常用选项与陷阱  
#### 1.3.2 文件查找与批量处理  
- **P0** `find` 组合条件（`-name`, `-mtime`, `-size`）及 `-exec`/`xargs`  
- **P0** 实战：一行命令找出并压缩 3 天前的日志  

### 1.4 文本处理流水线  
#### 1.4.1 管道、重定向与标准 I/O  
- **P0** stdin, stdout, stderr，`2>&1`，`/dev/null`  
#### 1.4.2 grep 家族  
- **P0** `grep -E`，`-v`, `-c`, `-n`, `-r`  
- **P1** `zgrep` 直接搜索压缩日志  
#### 1.4.3 sed 流编辑器  
- **P0** 原地替换 `-i`，模式空间，a/i/c  
- **P0** 面试场景：批量替换配置文件 IP  
#### 1.4.4 awk 数据处理引擎  
- **P0** `-F` 分隔符，模式–动作，BEGIN/END  
- **P0** 实战：统计 Nginx 日志 Top 10 IP、平均响应时间  
#### 1.4.5 其他必备文本工具  
- **P0** `sort`, `uniq`, `wc`, `head`, `tail`, `cut`, `tr`  

### 1.5 实验辅助：单行循环与极简调试  
**说明**：为后续重复监控实验提供最简能力，系统脚本在第 9 章  
#### 1.5.1 `for` 与 `while` 循环基础  
- **P0** 单行循环执行命令序列  
#### 1.5.2 脚本执行与 `bash -x` 调试  
- **P0** `#!/bin/bash`，`chmod +x`，`bash -x` 查看执行过程  

---
 
# 第2章 权限、用户与安全基石  
**本章目标**：掌握文件权限与用户管理，理解特殊权限在容器安全中的延伸  

### 2.1 Linux 用户与组体系  
#### 2.1.1 用户、组与关键文件  
- **P0** `/etc/passwd`、`/etc/shadow`、`/etc/group` 字段解析  
- **P0** `useradd`，`usermod`，`passwd`，用户加解锁  
#### 2.1.2 切换身份与提权  
- **P0** `su` 与 `sudo` 区别，`visudo` 配置免密切换  
- **P0** `sudo` 日志与安全面试点  

### 2.2 文件权限模型  
#### 2.2.1 基本权限位  
- **P0** `rwx` 对文件与目录的不同含义  
- **P0** `chmod` 数字法与符号法，`chown`/`chgrp`  
#### 2.2.2 特殊权限  
- **P0** SUID、SGID、Sticky Bit 原理与安全风险  
- **P0** 面试题：为何 `passwd` 有 SUID？如何找出所有 SUID 文件？  
#### 2.2.3 默认权限与 umask  
- **P0** umask 计算规则，在脚本与守护进程中的影响  

---

# 第3章 进程：生命周期、状态与系统调用  
**本章目标**：理解进程本质，掌握状态机，用 `strace` 透视行为，夯实系统调用基础  

### 3.1 进程模型与生命周期  
#### 3.1.1 进程诞生：`fork()` + `exec()`  
- **P0** 写时复制（COW），简要系统调用序列  
#### 3.1.2 进程状态机  
- **P0** R、S、D、Z、T 详析，**不可中断睡眠 D 状态**与 I/O 关系（**预告第 6 章实验回补**）  
- **P0** 僵尸进程与孤儿进程，为何无法直接 kill 僵尸  
#### 3.1.3 进程关系  
- **P0** 父/子进程、进程组、会话，`pstree` 可视化  

### 3.2 进程监控与控制  
#### 3.2.1 进程快照 `ps`  
- **P0** `ps aux` / `ps -ef` 各列（VSZ、RSS、%MEM、STAT）精确含义  
#### 3.2.2 动态监控 `top`/`htop`  
- **P0** 交互排序，查看线程，定位高 CPU 进程  
#### 3.2.3 信号与终止  
- **P0** 常见信号（SIGTERM、SIGKILL、SIGHUP），`nohup`，`disown`  

### 3.3 用户态、内核态与 `strace` 初探  
#### 3.3.1 内核与系统调用  
- **P0** 内核是资源管理者，进程在用户态运行  
- **P0** 访问资源必须通过系统调用进入内核态  
#### 3.3.2 用 `strace` 观察系统调用  
- **P0** `strace -p <pid>` 捕获系统调用，追踪 `ls` 识别 `open`、`write` 等  
- **P0** 演示进程阻塞在某个系统调用上的现象  
#### 3.3.3 最常用系统调用速查（补给箱）  
- `open` / `openat` — 打开文件  
- `read` / `write` — 读写文件描述符  
- `mmap` — 映射内存或文件  
- `brk` — 调整堆空间  
- `futex` — 用户态锁依赖的快速互斥  
- `clone` — 创建线程或进程  
- `wait4` — 等待子进程  
- `connect` / `accept` — 网络连接  
- `epoll_wait` — 等待事件  

---

# 第4章 调度、线程与负载  
**本章目标**：掌握线程模型、调度器思想，彻底拆解负载平均值的面试拷问  

### 4.1 线程的内核视角  
#### 4.1.1 `clone()` 与 `task_struct`  
- **P0** 线程与进程的统一表示，资源共享的空间  
#### 4.1.2 线程查看与识别  
- **P0** `top -H`，`ps -eLf`，理解 PID 与 TID  

### 4.2 CFS 调度器与权重  
#### 4.2.1 完全公平调度器思想  
- **P1** 内核用 `vruntime` 追踪运行时间，红黑树选择下一个进程  
- **P1** 进程权重决定时间片比例，`cpu.shares` 就是权重的体现  
#### 4.2.2 调度域与多核  
- **P1** 理解调度域，知道为什么 `load average` 看整体而非单核  

### 4.3 负载平均值的真义  
#### 4.3.1 load average 的计算成分  
- **P0** 包含 R 状态 + D 状态进程数  
- **P0** “CPU 空闲但负载高”必然指向 I/O 阻塞  
#### 4.3.2 排障故事框架  
- **P0** 负载高 → `top` 看整体 → `ps` 查 D 状态 → `iostat` 验证磁盘  

---

# 第5章 内存管理：从虚拟内存到 OOM  
**本章目标**：正确解读内存指标，深入 OOM 杀手，为容器内存限制埋下操作伏笔  

### 5.1 虚拟内存抽象  
#### 5.1.1 进程地址空间  
- **P0** 代码、堆、栈、mmap 区域，`pmap` 查看  
#### 5.1.2 核心内存指标  
- **P0** `free -h`：buffer/cache、available 含义  
- **P0** `top` 中 VIRT、RES、SHR、%MEM 与 swap 关系  

### 5.2 内存回收与 OOM  
#### 5.2.1 内核回收与 `vm.swappiness`  
- **P1** kswapd 触发条件，swap 倾向调节  
#### 5.2.2 OOM 机制与日志查找  
- **P0** `oom_score`、`oom_adj` 决定杀谁  
- **P0** `dmesg`，`journalctl -k | grep -i oom`（最简用法，第 8 章详解）  
#### 5.2.3 cgroup 内存限制快照  
- **P0** 手动创建 cgroup 并设置 `memory.limit_in_bytes`，触发 OOM（原理在第 10 章揭示）  

### 5.3 内存泄漏定位  
#### 5.3.1 观测 RSS 增长  
- **P0** `smem`、`pmap -x`  
#### 5.3.2 用 `strace` 发现异常内存调用  
- **P0** 观察 `brk`、`mmap` 连续增长  

---

# 第6章 磁盘 I/O 与文件系统进阶  
**本章目标**：形成 I/O 瓶颈分析闭环，亲手验证 D 状态，深入文件系统结构  

### 6.1 存储设备与挂载管理  
#### 6.1.1 磁盘分区与文件系统创建  
- **P0** `lsblk`、`fdisk`、`mkfs` 概念，`mount`/`umount`  
- **P1** `/etc/fstab` 字段与 `mount -a`  
#### 6.1.2 磁盘空间监控  
- **P0** `df -h`、`df -i`（inode 使用率）、`du -sh *`  

### 6.2 文件系统内部结构  
#### 6.2.1 inode 与数据块映射  
- **P1** 直接/间接指针、extent 概念  
#### 6.2.2 小文件陷阱  
- **P1** 大量小文件耗尽 inode 的故障与监控  

### 6.3 I/O 观测与 D 状态实验  
#### 6.3.1 `iostat` 深度解读  
- **P0** `iostat -x 1`：await、%util 饱和度判断  
#### 6.3.2 `iotop` / `pidstat -d`  
- **P0** 定位高 I/O 进程  
#### 6.3.3 模拟 I/O 阻塞制造 D 状态（闭环第 3 章悬念）  
- **P0** 使用附录提供的简易故障注射脚本，或挂载慢响应 NFS，观察负载飙升  
- **P0** 用 `strace` 验证进程阻塞在 `open`/`read`  

---

# 第7章 Linux 网络：从包到服务的必经路  
**本章目标**：独立解决网络不通，掌握高并发模型，覆盖 conntrack 与 DNS 典型排障  

### 7.1 网络接口与 TCP/IP 栈  
#### 7.1.1 iproute2 基础  
- **P0** `ip addr`、`ip link`、`ip route`  
#### 7.1.2 连接状态分析  
- **P0** `ss -tlnp`，TCP 状态机：TIME_WAIT、CLOSE_WAIT 堆积  
- **P0** 三次握手与四次挥手抓包  
#### 7.1.3 epoll 与高并发模型  
- **P1** 阻塞 I/O → 非阻塞 → epoll 演进  
- **P1** Nginx/Redis 的事件驱动思想简述  

### 7.2 防火墙与连接跟踪  
#### 7.2.1 iptables 表与链  
- **P0** filter、nat、mangle 表，包流向图  
#### 7.2.2 规则编写与排查  
- **P0** ACCEPT/DNAT/SNAT 规则，`iptables -L -n -v`  
#### 7.2.3 conntrack 表满故障（新增高频点）  
- **P0** 观察 `/proc/net/nf_conntrack`，`conntrack -S`  
- **P0** `nf_conntrack: table full` 丢包现象，调整 `nf_conntrack_max`  

### 7.3 网络排障工具组合  
#### 7.3.1 连通性测试  
- **P0** `ping`、`mtr`  
#### 7.3.2 DNS 故障排查  
- **P1** `dig` + `tcpdump port 53`  
- **P1** `/etc/resolv.conf` 超时、`ndots:5` 导致的服务雪崩案例  
#### 7.3.3 `tcpdump` 精通  
- **P0** 过滤、写文件、读回，识别重传与 RST  

### 7.4 网络命名空间与容器网络初探  
#### 7.4.1 网络命名空间与 veth pair  
- **P0** `ip netns` 创建两个隔离空间并连通，模拟网桥  
- **注**：命名空间全貌在第 10 章收网  
#### 7.4.2 容器端口映射与 iptables  
- **P1** 观察 DNAT 规则实现对外暴露  

---

# 第8章 systemd 与系统启动  
**本章目标**：掌握服务管理、日志体系，彻底回补 `journalctl`  

### 8.1 系统启动流程  
#### 8.1.1 从固件到 systemd  
- **P0** BIOS/UEFI → GRUB → kernel → initramfs → systemd  
#### 8.1.2 target 概念  
- **P0** `systemctl list-units`，与运行级别对比  

### 8.2 编写服务与定时器  
#### 8.2.1 自定义 Unit 文件  
- **P0** `[Unit]`、`[Service]`、`[Install]`，Type、ExecStart、Restart  
#### 8.2.2 定时器 Timer  
- **P1** 替代 cron 的 systemd timer  

### 8.3 journald 日志系统精讲  
#### 8.3.1 journalctl 高级过滤  
- **P0** 按服务、时间、内核日志过滤  
#### 8.3.2 持久化与清理  
- **P1** 配置磁盘存储与清理策略  

---

# 第9章 Shell 脚本进阶与自动化  
**本章目标**：从“能写”到“可靠”，完成生产级巡检脚本  

### 9.1 脚本健壮性  
#### 9.1.1 严格模式与错误处理  
- **P0** `set -euo pipefail`  
#### 9.1.2 信号捕获与锁  
- **P1** `trap`，`flock` 防并发执行  
#### 9.1.3 结构化输出  
- **P1** 日志分级与返回值  

### 9.2 实战巡检脚本  
#### 9.2.1 全栈信息收集  
- **P0** CPU/内存/磁盘/进程/端口巡检，彩色告警  
#### 9.2.2 参数化与配置分离  
- **P0** 利用环境变量或配置文件  

### 9.3 Ansible 入门理念  
- **P1** 无 agent，幂等性，playbook 批量推送配置  

---

# 第10章 容器技术基石：Namespace、Cgroups、Capabilities 与 OverlayFS  
**本章目标**：一体化掌握容器隔离、资源限制与安全机制，回补全书伏笔  

### 10.1 Namespace 全貌  
#### 10.1.1 七种命名空间  
- **P0** PID、NET、MNT、UTS、IPC、USER、Cgroup 的隔离目标  
#### 10.1.2 命名空间操作  
- **P0** `lsns`，`nsenter`，扩展第 7 章实验到多 namespace 协同  

### 10.2 Cgroups 深度控制  
#### 10.2.1 Cgroup v1/v2 结构与子系统  
- **P0** 挂载点，cpu, memory, blkio 控制器  
#### 10.2.2 资源限制实战  
- **P0** 内存和 CPU 配额设置，关联第 5 章 OOM 实验  
- **P0** `cpu.shares` 与 CFS 权重的关系（呼应第 4 章）  

### 10.3 容器与 Capabilities  
#### 10.3.1 Capabilities 机制  
- **P0** 超级用户权限拆分，`CAP_NET_RAW`、`CAP_SYS_ADMIN` 等能力位  
#### 10.3.2 容器安全配置  
- **P0** Docker/K8s 通过 `cap-drop`/`cap-add` 限制容器 root 权限  
- **P0** 演示：移除 `CAP_NET_RAW` 阻止 ping  

### 10.4 联合文件系统 OverlayFS  
#### 10.4.1 分层与写时复制  
- **P1** lowerdir、upperdir、merged、workdir  
- **P1** 对应 `docker image inspect` 查看层  

---

# 第11章 内核调优与高级追踪  
**本章目标**：组装性能分析武器库，为面试的“系统调优”问题备足弹药  

### 11.1 内核参数速调  
#### 11.1.1 核心调优点  
- **P0** `vm.swappiness`、`net.core.somaxconn`、`tcp_tw_reuse`、`nf_conntrack_max`  
#### 11.1.2 持久化配置  
- **P0** `/etc/sysctl.conf` 与 `sysctl -p`  

### 11.2 动态追踪工具链  
#### 11.2.1 `strace` 进阶  
- **P0** `-f`、`-e`、`-c` 统计系统调用耗时  
#### 11.2.2 `perf` 与火焰图  
- **P1** `perf top`/`perf record`，火焰图生成流程  
#### 11.2.3 eBPF 与 bcc-tools 入门  
- **P2** `execsnoop`、`opensnoop` 示例  

---

# 第12章 面试高频考点与故事化应答  
**本章目标**：将所有知识串成排障故事，自信面对任何 Linux 深问  

### 12.1 经典面试题深度拆解  
- “CPU 空闲但负载高” → D 状态、`iostat` 证据  
- “进程消失” → OOM/信号/段错误/手动 kill 的证据链  
- “HTTP 请求的 Linux 旅程” → 网卡、netfilter、路由、TCP、epoll  
- “容器为何轻量又安全” → Namespace、Cgroups、Capabilities  
- “连接频繁断开/丢包” → conntrack 表满、`ss` TIME_WAIT  
- “服务发现偶发超时” → DNS `/etc/resolv.conf` 与 `ndots` 排查  

### 12.2 构建排错故事库  
- **P0** 亲手制造故障并记录：内存泄漏、端口冲突、I/O 饱和、conntrack 满  
- **P0** 按“现象→假设→工具验证→结论”框架梳理  

---
