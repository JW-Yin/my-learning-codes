## ✅ 部署流程梳理（从零到 3 MON + 3 MGR 正常运行）

以下为在新宿主机上从零开始部署 **3 个 MON 容器 + 3 个 MGR 容器** 的完整步骤，**所有命令均已修正过路径问题，可直接复制粘贴执行**。

### 0. 前提条件
- 已安装 Docker、`ceph-base` / `ceph-common`（提供 `ceph-authtool`、`monmaptool`）。
- 宿主机 `/etc/ceph`、`/var/lib/ceph*` 无残留数据。

---

### 1. 环境准备
```bash
# 清理（如新机器可跳过）
docker ps -a | grep ceph- | awk '{print $1}' | xargs -r docker rm -f
docker network rm ceph-public 2>/dev/null || true
sudo rm -rf /etc/ceph /var/lib/ceph* /tmp/ceph* /tmp/monmap

# 拉取镜像、创建网络、生成 FSID
docker pull quay.io/ceph/ceph:v17.2.8
docker network create --subnet=192.168.206.0/24 ceph-public
export FSID=$(uuidgen)
echo "集群 FSID: $FSID"
```

### 2. 部署第一个 MON（ceph-node1）
```bash
sudo mkdir -p /etc/ceph /var/lib/ceph/mon/ceph-ceph-node1
sudo tee /etc/ceph/ceph.conf <<EOF
[global]
fsid = $FSID
mon_initial_members = ceph-node1
mon_host = 192.168.206.151
public_network = 192.168.206.0/24
auth_cluster_required = cephx
auth_service_required = cephx
auth_client_required = cephx
osd_pool_default_size = 1
osd_pool_default_min_size = 1
EOF

sudo ceph-authtool --create-keyring /tmp/ceph.mon.keyring --gen-key -n mon. --cap mon 'allow *'
sudo ceph-authtool --create-keyring /etc/ceph/ceph.client.admin.keyring --gen-key -n client.admin \
  --cap mon 'allow *' --cap osd 'allow *' --cap mds 'allow *' --cap mgr 'allow *'
sudo ceph-authtool /tmp/ceph.mon.keyring --import-keyring /etc/ceph/ceph.client.admin.keyring
monmaptool --create --add ceph-node1 192.168.206.151 --fsid $FSID /tmp/monmap

sudo docker run --rm \
  -v /var/lib/ceph:/var/lib/ceph:z \
  -v /etc/ceph:/etc/ceph:z \
  -v /tmp:/tmp:z \
  quay.io/ceph/ceph:v17.2.8 \
  ceph-mon --mkfs -i ceph-node1 --monmap /tmp/monmap --keyring /tmp/ceph.mon.keyring \
  --public-addr 192.168.206.151

sudo chown -R 167:167 /var/lib/ceph /etc/ceph

docker run -d \
  --name ceph-mon-ceph-node1 \
  --network ceph-public \
  --ip 192.168.206.151 \
  -v /var/lib/ceph:/var/lib/ceph:z \
  -v /etc/ceph:/etc/ceph:z \
  -e MON_IP=192.168.206.151 \
  quay.io/ceph/ceph:v17.2.8 \
  ceph-mon -i ceph-node1 -f

sleep 10
docker exec ceph-mon-ceph-node1 ceph -s   # 确认单 MON 正常
```

### 3. 部署第二个 MON（ceph-node2）
```bash
docker exec ceph-mon-ceph-node1 ceph mon getmap -o /tmp/monmap
docker cp ceph-mon-ceph-node1:/tmp/monmap /tmp/monmap

sudo mkdir -p /etc/ceph-mon-ceph-node2 /var/lib/ceph-mon-ceph-node2/mon/ceph-ceph-node2
sudo cp /etc/ceph/ceph.client.admin.keyring /etc/ceph-mon-ceph-node2/
sudo cp /tmp/ceph.mon.keyring /etc/ceph-mon-ceph-node2/
sudo cp /tmp/monmap /etc/ceph-mon-ceph-node2/

sudo tee /etc/ceph-mon-ceph-node2/ceph.conf <<EOF
[global]
fsid = $FSID
mon_initial_members = ceph-node1,ceph-node2
mon_host = 192.168.206.151,192.168.206.152
public_network = 192.168.206.0/24
auth_cluster_required = cephx
auth_service_required = cephx
auth_client_required = cephx
osd_pool_default_size = 1
osd_pool_default_min_size = 1
EOF

sudo docker run --rm \
  -v /var/lib/ceph-mon-ceph-node2:/var/lib/ceph:z \
  -v /etc/ceph-mon-ceph-node2:/etc/ceph:z \
  -v /tmp:/tmp:z \
  quay.io/ceph/ceph:v17.2.8 \
  ceph-mon --mkfs -i ceph-node2 --monmap /tmp/monmap --keyring /tmp/ceph.mon.keyring \
  --public-addr 192.168.206.152

sudo chown -R 167:167 /var/lib/ceph-mon-ceph-node2 /etc/ceph-mon-ceph-node2

docker run -d \
  --name ceph-mon-ceph-node2 \
  --network ceph-public \
  --ip 192.168.206.152 \
  -v /var/lib/ceph-mon-ceph-node2:/var/lib/ceph:z \
  -v /etc/ceph-mon-ceph-node2:/etc/ceph:z \
  -e MON_IP=192.168.206.152 \
  quay.io/ceph/ceph:v17.2.8 \
  ceph-mon -i ceph-node2 -f

# 更新 node1 配置并重启
sudo sed -i 's/mon_initial_members = ceph-node1/mon_initial_members = ceph-node1,ceph-node2/' /etc/ceph/ceph.conf
sudo sed -i 's/mon_host = 192.168.206.151/mon_host = 192.168.206.151,192.168.206.152/' /etc/ceph/ceph.conf
docker restart ceph-mon-ceph-node1

sleep 10
docker exec ceph-mon-ceph-node1 ceph -s   # 确认双 MON 仲裁
```

### 4. 部署第三个 MON（ceph-node3）
```bash
docker exec ceph-mon-ceph-node1 ceph mon getmap -o /tmp/monmap
docker cp ceph-mon-ceph-node1:/tmp/monmap /tmp/monmap

sudo mkdir -p /etc/ceph-mon-ceph-node3 /var/lib/ceph-mon-ceph-node3/mon/ceph-ceph-node3
sudo cp /etc/ceph/ceph.client.admin.keyring /etc/ceph-mon-ceph-node3/
sudo cp /tmp/ceph.mon.keyring /etc/ceph-mon-ceph-node3/
sudo cp /tmp/monmap /etc/ceph-mon-ceph-node3/

sudo tee /etc/ceph-mon-ceph-node3/ceph.conf <<EOF
[global]
fsid = $FSID
mon_initial_members = ceph-node1,ceph-node2,ceph-node3
mon_host = 192.168.206.151,192.168.206.152,192.168.206.153
public_network = 192.168.206.0/24
auth_cluster_required = cephx
auth_service_required = cephx
auth_client_required = cephx
osd_pool_default_size = 1
osd_pool_default_min_size = 1
EOF

sudo docker run --rm \
  -v /var/lib/ceph-mon-ceph-node3:/var/lib/ceph:z \
  -v /etc/ceph-mon-ceph-node3:/etc/ceph:z \
  -v /tmp:/tmp:z \
  quay.io/ceph/ceph:v17.2.8 \
  ceph-mon --mkfs -i ceph-node3 --monmap /tmp/monmap --keyring /tmp/ceph.mon.keyring \
  --public-addr 192.168.206.153

sudo chown -R 167:167 /var/lib/ceph-mon-ceph-node3 /etc/ceph-mon-ceph-node3

docker run -d \
  --name ceph-mon-ceph-node3 \
  --network ceph-public \
  --ip 192.168.206.153 \
  -v /var/lib/ceph-mon-ceph-node3:/var/lib/ceph:z \
  -v /etc/ceph-mon-ceph-node3:/etc/ceph:z \
  -e MON_IP=192.168.206.153 \
  quay.io/ceph/ceph:v17.2.8 \
  ceph-mon -i ceph-node3 -f

# 更新 node1/node2 配置文件
sudo sed -i 's/mon_initial_members = .*/mon_initial_members = ceph-node1,ceph-node2,ceph-node3/' /etc/ceph/ceph.conf /etc/ceph-mon-ceph-node2/ceph.conf
sudo sed -i 's/mon_host = .*/mon_host = 192.168.206.151,192.168.206.152,192.168.206.153/' /etc/ceph/ceph.conf /etc/ceph-mon-ceph-node2/ceph.conf
docker restart ceph-mon-ceph-node1 ceph-mon-ceph-node2

sleep 15
docker exec ceph-mon-ceph-node1 ceph -s   # 确认三 MON 仲裁
```

### 5. 部署三个 MGR（⚠️ 使用已修正的正确目录结构）
```bash
# MGR node1
docker exec ceph-mon-ceph-node1 ceph auth get-or-create mgr.ceph-node1 \
  mon 'allow profile mgr' osd 'allow *' mds 'allow *' \
  -o /etc/ceph/ceph.mgr.ceph-node1.keyring
sudo mkdir -p /var/lib/ceph-mgr-ceph-node1/ceph-ceph-node1
sudo cp /etc/ceph/ceph.mgr.ceph-node1.keyring /var/lib/ceph-mgr-ceph-node1/ceph-ceph-node1/keyring
sudo chown -R 167:167 /var/lib/ceph-mgr-ceph-node1
docker run -d \
  --name ceph-mgr-ceph-node1 \
  --network ceph-public \
  -v /etc/ceph:/etc/ceph:z \
  -v /var/lib/ceph-mgr-ceph-node1:/var/lib/ceph/mgr:z \
  quay.io/ceph/ceph:v17.2.8 \
  ceph-mgr -i ceph-node1 -f

# MGR node2
docker exec ceph-mon-ceph-node1 ceph auth get-or-create mgr.ceph-node2 \
  mon 'allow profile mgr' osd 'allow *' mds 'allow *' \
  -o /etc/ceph/ceph.mgr.ceph-node2.keyring
sudo mkdir -p /var/lib/ceph-mgr-ceph-node2/ceph-ceph-node2
sudo cp /etc/ceph/ceph.mgr.ceph-node2.keyring /var/lib/ceph-mgr-ceph-node2/ceph-ceph-node2/keyring
sudo chown -R 167:167 /var/lib/ceph-mgr-ceph-node2
docker run -d \
  --name ceph-mgr-ceph-node2 \
  --network ceph-public \
  -v /etc/ceph:/etc/ceph:z \
  -v /var/lib/ceph-mgr-ceph-node2:/var/lib/ceph/mgr:z \
  quay.io/ceph/ceph:v17.2.8 \
  ceph-mgr -i ceph-node2 -f

# MGR node3
docker exec ceph-mon-ceph-node1 ceph auth get-or-create mgr.ceph-node3 \
  mon 'allow profile mgr' osd 'allow *' mds 'allow *' \
  -o /etc/ceph/ceph.mgr.ceph-node3.keyring
sudo mkdir -p /var/lib/ceph-mgr-ceph-node3/ceph-ceph-node3
sudo cp /etc/ceph/ceph.mgr.ceph-node3.keyring /var/lib/ceph-mgr-ceph-node3/ceph-ceph-node3/keyring
sudo chown -R 167:167 /var/lib/ceph-mgr-ceph-node3
docker run -d \
  --name ceph-mgr-ceph-node3 \
  --network ceph-public \
  -v /etc/ceph:/etc/ceph:z \
  -v /var/lib/ceph-mgr-ceph-node3:/var/lib/ceph/mgr:z \
  quay.io/ceph/ceph:v17.2.8 \
  ceph-mgr -i ceph-node3 -f

sleep 15
docker exec ceph-mon-ceph-node1 ceph -s   # 确认 MGR 上线（一个 active，两个 standby）
```

---

### ✅ 当前状态确认
- **MON**：3 个容器，仲裁正常
- **MGR**：3 个容器，一个 active，两个 standby
- **集群健康**：可能存在无害警告（`mons are allowing insecure global_id reclaim` 等），可通过以下命令消除：
  ```bash
  docker exec ceph-mon-ceph-node1 ceph mon enable-msgr2
  docker exec ceph-mon-ceph-node1 ceph config set mon auth_allow_insecure_global_id_reclaim false
  ```

---

### 📦 容器清单
| 容器名 | IP | 服务 |
|--------|----|------|
| `ceph-mon-ceph-node1` | 192.168.206.151 | MON |
| `ceph-mon-ceph-node2` | 192.168.206.152 | MON |
| `ceph-mon-ceph-node3` | 192.168.206.153 | MON |
| `ceph-mgr-ceph-node1` | - | MGR |
| `ceph-mgr-ceph-node2` | - | MGR |
| `ceph-mgr-ceph-node3` | - | MGR |

---

### 🚀 下一步（OSD 部署）
当前集群已具备高可用控制平面，但尚无存储容量。接下来需要部署 **3 个独立 OSD 容器**。是否需要我继续提供 OSD 部署的完整命令清单（同样经过路径修正）？