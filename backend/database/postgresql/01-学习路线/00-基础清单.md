# 《PostgreSQL 从入门到精通：现代生产级实战体系》

## 第一部分：入门奠基篇（从安装到第一行 SQL）

# 第1章 初识与安装 PostgreSQL
### 1.1 关系型数据库与 PostgreSQL 生态
#### 1.1.1 关系模型与 SQL 标准回顾
- **核心关键词**：表、行、列、ACID、SQL 标准演进
#### 1.1.2 PostgreSQL 发展简史与定位
- **核心关键词**：Ingres 起源、开源社区、与 MySQL/Oracle 的对比定位

### 1.2 环境搭建与客户端
#### 1.2.1 多平台安装指南
- **核心关键词**：Windows 安装包、Ubuntu apt、CentOS yum、Docker 容器一键部署
#### 1.2.2 psql 命令行完全指南
- **核心关键词**：连接参数、元命令（`\l` `\c` `\dt` `\d`）、执行 SQL 文件、`\x` 扩展显示
#### 1.2.3 图形化管理工具
- **核心关键词**：pgAdmin 4 界面导航、DBeaver 通用连接

# 第2章 数据库与表的初步操作
### 2.1 创建第一个数据库与表
#### 2.1.1 数据库与模式的概念
- **核心关键词**：`CREATE DATABASE`、`CREATE SCHEMA`、`search_path` 搜索路径
#### 2.1.2 数据类型快速入门
- **核心关键词**：`INTEGER`、`TEXT`、`VARCHAR`、`TIMESTAMP`、`BOOLEAN`
#### 2.1.3 创建第一张表
- **核心关键词**：`CREATE TABLE`、`PRIMARY KEY`、`NOT NULL`、`DEFAULT`

### 2.2 数据的增删改查基础
#### 2.2.1 插入与导入数据
- **核心关键词**：`INSERT INTO VALUES`、`COPY FROM` 批量导入、CSV 处理
#### 2.2.2 基础查询与过滤
- **核心关键词**：`SELECT ... FROM ... WHERE ...`、`ORDER BY`、`LIMIT`
#### 2.2.3 更新与删除数据
- **核心关键词**：`UPDATE ... SET`、`DELETE FROM`、`TRUNCATE` 清空表

## 第二部分：核心开发进阶篇（表设计与高效 SQL）

# 第3章 数据类型深度解析
### 3.1 数值与字符串类型精讲
#### 3.1.1 精确数值与自增主键
- **核心关键词**：`NUMERIC(precision, scale)` 避免浮点误差、`SERIAL` / `IDENTITY` 自增列
#### 3.1.2 字符类型选型建议
- **核心关键词**：`TEXT` vs `VARCHAR(n)` vs `CHAR(n)`、性能差异与存储策略

### 3.2 时间与日期处理艺术
#### 3.2.1 时间戳与时区陷阱
- **核心关键词**：`TIMESTAMP` vs `TIMESTAMPTZ`、`AT TIME ZONE` 转换、`INTERVAL` 运算
#### 3.2.2 常用日期函数
- **核心关键词**：`date_trunc` 截断、`EXTRACT` 提取、`AGE` 计算年龄

### 3.3 数组与网络地址类型
#### 3.3.1 数组的存储与查询
- **核心关键词**：`INT[]`、`UNNEST` 展开数组、数组包含操作符 `@>`
#### 3.3.2 网络地址类型实用场景
- **核心关键词**：`INET` 存 IP、`CIDR` 网段判断、`MACADDR` 物理地址

### 3.4 约束与数据完整性
#### 3.4.1 常规约束与自定义域
- **核心关键词**：`CHECK` 约束、`UNIQUE` 唯一索引、`DOMAIN` 业务域定义

### 3.5 JSON 与 JSONB 基础实战
#### 3.5.1 JSON 与 JSONB 的区别与选择
- **核心关键词**：存储效率、查询性能、JSONB 的二进制优势
#### 3.5.2 JSONB 核心操作符入门
- **核心关键词**：`->` 提取对象、`->>` 提取文本、`@>` 包含判断、`jsonb_set` 更新字段
#### 3.5.3 JSONB 索引初步
- **核心关键词**：GIN 索引加速、`jsonb_path_ops` 轻量索引
- **📌 阅读指引**：本节仅演示索引创建语法，GIN 索引的底层原理和详细性能分析请参阅 **第6章 6.1.2 节**。
- **⚠️ 性能警示**：若表中数据量超过数万行，未建索引的 JSONB 查询将导致全表扫描。请先阅读第 6 章 GIN 索引原理后再进行大规模数据测试。

# 第4章 高级表定义与分区
### 4.1 表继承机制
#### 4.1.1 继承的基本语法与行为
- **核心关键词**：`INHERITS`、`ONLY` 关键字、继承链查询
#### 4.1.2 继承的局限与声明式分区的关系
- **核心关键词**：继承 vs 分区、为何官方推荐声明式分区
> **📌 阅读提示**：本节为理解声明式分区提供历史背景，纯应用开发读者可选择性阅读。

### 4.2 声明式分区表实战
#### 4.2.1 分区策略选择
- **核心关键词**：`RANGE` 范围分区、`LIST` 列表分区、`HASH` 哈希分区
#### 4.2.2 分区表的管理与维护
- **核心关键词**：`ATTACH PARTITION`、`DETACH PARTITION`、默认分区、子分区
#### 4.2.3 分区裁剪与性能提升
- **核心关键词**：`partition pruning`、`enable_partition_pruning` 参数、可通过 `EXPLAIN` 查看分区裁剪情况（`EXPLAIN` 详见第 6 章）

### 4.3 分区表的最佳实践与典型场景
- **核心关键词**：时序数据分区、日志归档分区、大表拆分策略

# 第5章 表关联与高级查询
### 5.1 多表连接查询实战
#### 5.1.1 内连接与外连接
- **核心关键词**：`INNER JOIN`、`LEFT JOIN`、`RIGHT JOIN`、`FULL OUTER JOIN`
#### 5.1.2 自连接与表别名
- **核心关键词**：同一张表的多重关联、`AS` 别名简化
#### 5.1.3 子查询与派生表
- **核心关键词**：`WHERE IN (子查询)`、`FROM (子查询) AS t`、相关子查询

### 5.2 公共表表达式与递归查询
#### 5.2.1 非递归 CTE 优化可读性
- **核心关键词**：`WITH` 子句临时视图、多 CTE 链式调用
#### 5.2.2 递归查询处理树状数据
- **核心关键词**：`WITH RECURSIVE`、组织架构层级、评论回复链

### 5.3 窗口函数与分组运算
#### 5.3.1 排名与分布函数
- **核心关键词**：`ROW_NUMBER()`、`RANK()`、`DENSE_RANK()`、`NTILE(n)`
#### 5.3.2 偏移与滑动窗口
- **核心关键词**：`LAG/LEAD` 环比分析、`OVER (PARTITION BY ... ORDER BY ...)`
#### 5.3.3 高级分组聚合
- **核心关键词**：`GROUPING SETS`、`ROLLUP` 小计、`CUBE` 多维分析、`FILTER` 条件计数

## 第三部分：性能优化实战篇（让数据库飞起来）

# 第6章 索引设计与执行计划解读
### 6.1 索引算法与最佳实践
#### 6.1.1 B-Tree 默认索引精讲
- **核心关键词**：索引创建时机、联合索引字段顺序、覆盖索引
#### 6.1.2 GIN 倒排索引处理复杂类型
- **核心关键词**：JSONB 加速、数组查询加速、全文检索 `tsvector`
- **📌 回顾链接**：本节深化第 3.5 节 JSONB 索引的底层原理，建议回顾后再读。
#### 6.1.3 GiST 与 SP-GiST 空间索引
- **核心关键词**：几何类型、范围类型、PostGIS 前置依赖
- **📌 阅读指引**：PostGIS 是 GiST 索引最经典的应用场景，详细使用请参阅 **第12章 12.2 节**。
#### 6.1.4 BRIN 块范围索引的威力
- **核心关键词**：时序数据、极低维护成本、物理存储相关性

### 6.2 读懂执行计划
#### 6.2.1 `EXPLAIN` 输出参数详解
- **核心关键词**：`ANALYZE` 真实耗时、`BUFFERS` 缓存命中率、`COST` 成本估算
#### 6.2.2 扫描方式与连接策略识别
- **核心关键词**：`Seq Scan` vs `Index Scan`、`Index Only Scan` 条件、`Bitmap Scan` 机制
#### 6.2.3 强制关闭特定扫描的调试技巧
- **核心关键词**：`enable_seqscan` 等调试参数、帮助判断索引优劣

# 第7章 服务器配置与 SQL 调优
### 7.1 关键内存与代价参数调优
#### 7.1.1 共享内存与工作内存
- **核心关键词**：`shared_buffers` 调整策略、`work_mem` 对排序的影响、`maintenance_work_mem`
#### 7.1.2 查询优化器参数修正
- **核心关键词**：`random_page_cost` (HDD vs SSD)、`effective_cache_size`、并行开关 `parallel`

### 7.2 必备扩展：pg_stat_statements 深度解析
#### 7.2.1 安装与配置
- **核心关键词**：`shared_preload_libraries`、重启加载、重置统计
#### 7.2.2 核心字段解读与慢查询定位
- **核心关键词**：`calls` 调用次数、`total_time` 总耗时、`mean_time` 平均耗时、`stddev` 波动率

### 7.3 并行查询深度调优
#### 7.3.1 并行查询触发条件与限制
- **核心关键词**：`min_parallel_table_scan_size`、`max_parallel_workers_per_gather`、`Gather` 节点识别
#### 7.3.2 并行不安全的函数排查
- **📌 概念前置**：PostgreSQL 中的函数（Function）将在第 11 章详细讲解。此处你只需知道：某些函数不能被并行执行，可通过 `EXPLAIN VERBOSE` 查看执行计划中是否有 `Parallel Aware` 标记来初步判断。
- **核心关键词**：`PARALLEL SAFE` 标记、`EXPLAIN VERBOSE` 查看并行信息

## 第四部分：内核原理与事务篇（从表象到底层）

> **📌 本部分价值指引**：理解 MVCC、VACUUM 和 WAL，是你从“会用 SQL”跨越到“掌控 PostgreSQL”的唯一路径。掌握本部分内容，你将能够解决生产环境中 90% 的诡异慢查询、磁盘告警和并发故障。请勿跳过。

# 第8章 事务与并发控制
> **📌 故事引入**：某电商大促夜，库存扣减出现“超卖”——明明库存为 1，却有两个用户同时下单成功。真相隐藏在 PostgreSQL 的 Xmin 和 Xmax 之中。本章将为你揭开并发控制的神秘面纱。

### 8.1 事务基础与隔离级别
#### 8.1.1 事务的基本命令
- **核心关键词**：`BEGIN`、`COMMIT`、`ROLLBACK`、`SAVEPOINT` 部分回滚
#### 8.1.2 PostgreSQL 四种隔离级别
- **核心关键词**：`Read Committed` 默认级别、`Repeatable Read` 无幻读、`Serializable` 串行化
#### 8.1.3 显式锁定与并发技巧
- **核心关键词**：`FOR UPDATE` 行锁、`SKIP LOCKED` 队列处理、`NOWAIT` 立即失败

### 8.2 MVCC 多版本并发控制深度揭秘
#### 8.2.1 元组可见性与系统隐藏列
- **核心关键词**：`Xmin`、`Xmax`、`cmin/cmax`、快照 `Snapshot` 导出分析
#### 8.2.2 从 Update 看 MVCC 行为
- **核心关键词**：Update = Delete + Insert 的底层演示、Hot Update 优化

# 第9章 存储引擎与清理机制
### 9.1 物理存储结构
#### 9.1.1 堆表文件与页结构
- **核心关键词**：Page 页头、`ItemId` 指针、`HeapTupleHeader` 布局、`TOAST` 超大字段存储
#### 9.1.2 表空间与文件布局
- **核心关键词**：`pg_tblspc`、数据库集群目录结构、`oid2name` 定位文件

### 9.2 VACUUM 与表膨胀治理
#### 9.2.1 死元组产生与空间回收
- **核心关键词**：表膨胀现象、`VACUUM` 标记重用、`VACUUM FULL` 重建文件
#### 9.2.2 Autovacuum 自动化调优
- **核心关键词**：`autovacuum_vacuum_threshold`、`autovacuum_vacuum_scale_factor`、监控 `pg_stat_user_tables`

# 第10章 WAL 日志与崩溃恢复
> **📌 阅读提示**：本章内容偏底层原理，纯应用开发读者可选择性阅读，但理解 WAL 将极大提升你对备份恢复和高可用架构的理解深度。

### 10.1 预写日志机制
#### 10.1.1 WAL 写入与同步级别
- **核心关键词**：`LSN` 日志序列号、`WAL Writer`、`synchronous_commit` 参数权衡
### 10.2 检查点与后台写进程
#### 10.2.1 检查点触发与性能抖动
- **核心关键词**：`checkpoint_completion_target`、`min_wal_size`、`max_wal_size`

## 第五部分：开发扩展与自动化篇

# 第11章 存储过程与触发器开发
### 11.1 PL/pgSQL 编程基础
#### 11.1.1 函数基本结构与返回类型
- **核心关键词**：`CREATE FUNCTION`、`RETURNS`、`LANGUAGE plpgsql`
#### 11.1.2 控制流与动态 SQL
- **核心关键词**：`IF ... THEN ... ELSE`、`LOOP`、`EXECUTE` 动态拼接
#### 11.1.3 触发器与事件触发
- **核心关键词**：`CREATE TRIGGER`、`NEW` / `OLD` 记录、审计日志实现
- **💡 回顾**：触发器的 `OLD` 和 `NEW` 伪记录，本质上是 MVCC 中同一行数据的两个版本，可回顾第 8 章 8.2.2 节。

### 11.2 存储过程与事务管理
- **📌 前置回顾**：本节涉及事务控制，PostgreSQL 的事务隔离级别与 MVCC 原理已在 **第8章** 详细讲解，建议读者适时回顾。
#### 11.2.1 存储过程 (`PROCEDURE`) 与函数 (`FUNCTION`) 的差异
- **核心关键词**：`CALL` 调用、事务控制 (`COMMIT` / `ROLLBACK` inside proc)
#### 11.2.2 使用 PL/pgSQL 实现复杂业务逻辑
- **核心关键词**：异常捕获 `EXCEPTION`、批量数据处理循环

# 第12章 内置全文检索与流行扩展实战
### 12.1 内置全文检索实战
- **📌 概念速览**：全文检索的核心数据类型是 `tsvector`（文本搜索向量）和 `tsquery`（文本搜索查询）。本节将带你从分词到排序完整掌握 PG 的全文检索能力。
#### 12.1.1 分词与向量构建
- **核心关键词**：`to_tsvector`、`zhparser` 中文分词、`tsquery` 语法
#### 12.1.2 权重排序与高亮
- **核心关键词**：`setweight`、`ts_rank`、`ts_headline` 搜索结果摘要

### 12.2 地理空间扩展：PostGIS 精讲
#### 12.2.1 几何类型与空间关系函数
- **核心关键词**：`GEOMETRY`、`GEOGRAPHY`、`ST_Distance`、`ST_Contains`、`ST_Transform`
#### 12.2.2 空间索引与应用场景
- **核心关键词**：GiST 空间索引加速、LBS 距离排序优化

### 12.3 AI 向量扩展：pgvector 实战
#### 12.3.1 向量数据类型与相似度计算
- **核心关键词**：`vector(1536)` 定义维度、`<=>` 余弦距离、L2 距离、内积
#### 12.3.2 向量索引与 RAG 演示
- **核心关键词**：索引选型方法论（IVFFlat 适合静态数据，HNSW 适合动态数据）、`ORDER BY ... LIMIT` 近似检索、结合 JSONB 实现文档问答元数据管理
- **📌 版本提示**：pgvector 的索引参数随版本演进较快，本节侧重选型方法论，具体参数请查阅最新官方文档。

### 12.4 联邦查询：外部数据包装器 (FDW) 入门
#### 12.4.1 FDW 基本原理与架构
- **核心关键词**：外部表、Foreign Server、User Mapping
#### 12.4.2 常用 FDW 实战
- **核心关键词**：`postgres_fdw` 跨库查询、`mysql_fdw` 异构数据源、`file_fdw` 读取 CSV
#### 12.4.3 FDW 的写入与事务支持
- **核心关键词**：`IMPORT FOREIGN SCHEMA`、两阶段提交、分布式事务限制

### 12.5 PostgreSQL 扩展生态全景指南
#### 12.5.1 扩展安装与管理基础
- **核心关键词**：`CREATE EXTENSION`、`\dx` 查看已安装扩展、`shared_preload_libraries`、扩展升级

#### 12.5.2 本书详讲扩展速查
| 扩展名称 | 功能定位 | 详讲章节 |
| :--- | :--- | :--- |
| `pg_stat_statements` | 慢查询分析与统计 | 第 7.2 节 |
| `PostGIS` | 地理空间数据处理 | 第 12.2 节 |
| `pgvector` | AI 向量相似度检索 | 第 12.3 节 |
| `postgres_fdw` | 跨数据库联邦查询 | 第 12.4 节 |

#### 12.5.3 推荐扩展速查表（场景导航）
| 扩展名称 | 一句话功能 | 什么时候需要它 | 关键命令/用法 |
| :--- | :--- | :--- | :--- |
| `pg_cron` | 数据库内定时任务 | 定期清理分区、刷新物化视图、定时聚合 | `SELECT cron.schedule('job', '0 3 * * *', 'SQL');` |
| `pg_partman` | 分区表自动管理 | 时序数据自动创建/删除分区 | `SELECT create_parent('public.logs', 'created_at', 'native', 'daily');` |
| `pg_repack` | 在线重整表 | 表膨胀严重但无法忍受 `VACUUM FULL` 的锁表 | `pg_repack -t table_name -d dbname` |
| `pg_rman` | 物理备份增强 | 需要比 `pg_basebackup` 更灵活的增量备份和校验 | `pg_rman backup -B /backup; pg_rman validate` |
| `pgAudit` | 审计日志 | 详细记录 SQL 执行（等保合规） | `ALTER SYSTEM SET pgaudit.log = 'write,ddl';` |
| `wal2json` | WAL 变更捕获 | CDC 将数据变更实时推送 Kafka | `CREATE PUBLICATION pub FOR ALL TABLES;` |
| `pg_hint_plan` | 强制执行计划干预 | 优化器选错索引时手动指定执行计划 | `/*+ SeqScan(users) */ SELECT ...` |
| `pg_prewarm` | 缓存预热 | 重启或切换后提前加载热数据 | `SELECT pg_prewarm('table_name');` |
| `pg_trgm` | 模糊字符串匹配加速 | `LIKE '%keyword%'` 加速或相似度匹配 | `CREATE INDEX ON t USING GIN (col gin_trgm_ops);` |
| `btree_gin` / `btree_gist` | 混合索引支持 | 在 GIN/GiST 索引中包含标量字段 | 搭配 `pg_trgm` 使用 |
| `uuid-ossp` | UUID 生成 | 生成 UUID 主键 | `SELECT uuid_generate_v4();` |
| `tablefunc` | 交叉表查询 | 需要行转列（透视表） | `SELECT * FROM crosstab(...);` |
| `hstore` | 键值对存储 | 已被 JSONB 取代，仅遗留系统使用 | 不推荐新项目使用 |

#### 12.5.4 如何发现更多扩展？
- **PGXN**：PostgreSQL 扩展网络
- **`pg_available_extensions` 视图**：查看当前实例可用扩展
- **云厂商 RDS 支持列表**：AWS RDS、阿里云 RDS、腾讯云 PostgreSQL 均已内置大量常用扩展

## 第六部分：企业级运维与高可用篇

# 第13章 用户权限与安全策略
### 13.1 角色与权限体系
#### 13.1.1 角色管理与继承
- **核心关键词**：`CREATE ROLE`、`LOGIN` 属性、`WITH ADMIN OPTION`
#### 13.1.2 对象权限精细控制
- **核心关键词**：`GRANT ... ON ALL TABLES IN SCHEMA`、默认权限 `ALTER DEFAULT PRIVILEGES`
#### 13.1.3 行级安全策略 (RLS) 与 search_path 安全陷阱
- **核心关键词**：`ALTER TABLE ENABLE ROW LEVEL SECURITY`、`USING` 策略表达式、`search_path` 劫持防范、`public` 模式权限回收

### 13.2 连接加密与认证
- **核心关键词**：`pg_hba.conf` 配置详解、`SCRAM-SHA-256` 加密认证、SSL/TLS 证书配置

# 第14章 备份恢复、连接池与复制
### 14.1 逻辑与物理备份
#### 14.1.1 pg_dump 与 pg_restore 高级技巧
- **核心关键词**：自定义格式、并行导出、仅导出模式结构
#### 14.1.2 物理备份与 PITR 时间点恢复（适配 PG 12+）
- **核心关键词**：`pg_basebackup`、WAL 归档配置、`recovery.signal` 文件创建、`recovery_target_time` 精确回档

### 14.2 连接池管理：PgBouncer 实战
#### 14.2.1 为什么需要连接池
- **核心关键词**：进程模型开销、连接数爆炸、连接池的价值
#### 14.2.2 PgBouncer 安装与配置
- **核心关键词**：`pool_mode`（Session / Transaction / Statement）、`max_client_conn`、`default_pool_size`
#### 14.2.3 PgBouncer 监控与管理
- **核心关键词**：`SHOW POOLS`、`SHOW STATS`、Admin Console

### 14.3 物理流复制与高可用
#### 14.3.1 主备流复制搭建
- **核心关键词**：物理复制槽、同步复制与异步复制权衡、`pg_stat_replication` 监控
#### 14.3.2 高可用方案选型
- **核心关键词**：Patroni + etcd（传统环境）、CloudNativePG（K8s 环境）、VIP 漂移

### 14.4 逻辑复制与数据分发
#### 14.4.1 逻辑复制的架构与优势
- **核心关键词**：发布 (Publication)、订阅 (Subscription)、表级粒度、跨版本支持
#### 14.4.2 逻辑复制实战
- **核心关键词**：`CREATE PUBLICATION`、`CREATE SUBSCRIPTION`、冲突处理
#### 14.4.3 逻辑复制的典型应用场景
- **核心关键词**：零停机大版本升级、数据迁移、多活数据同步、审计订阅

# 第15章 日常监控与故障排查
### 15.1 统计视图与监控指标
#### 15.1.1 必备监控查询脚本
- **核心关键词**：`pg_stat_activity` 查活跃连接、`pg_locks` 查阻塞、`pg_stat_statements` 分析慢 SQL
#### 15.1.2 现代监控体系搭建
- **核心关键词**：PostgreSQL Exporter、Prometheus 指标采集、Grafana 仪表盘推荐

### 15.2 典型生产故障诊断
#### 15.2.1 CPU 利用率 100% 排查思路
- **核心关键词**：定位问题 SQL、分析执行计划、排查 JIT 编译影响
#### 15.2.2 磁盘 IO 飙升与连接数爆满
- **核心关键词**：`checkpoint` 风暴、`idle in transaction` 长事务检查

## 第七部分：架构设计与附录

# 第16章 PostgreSQL 综合场景方案设计
### 16.1 场景一：高并发论坛/内容社区设计
- **核心技术组合**：递归 CTE 查评论树、JSONB 存帖子扩展属性、全文检索搜帖子、GIN 索引加速、RLS 实现多租户隔离、PgBouncer 连接池

### 16.2 场景二：物联网时序数据平台设计
- **核心技术组合**：声明式 Range 分区（按天）、BRIN 索引降低存储成本、JSONB 存动态 Payload、`pg_cron` 定时聚合旧数据

### 16.3 场景三：跨数据库联邦查询架构
- **核心技术组合**：`postgres_fdw` 跨库联查、外部表物化视图、异构数据源聚合

## 附录
- **附录 A**：PostgreSQL 配置参数调优速查表
- **附录 B**：`psql` 常用元命令速查卡
- **附录 C**：云上 PostgreSQL 选型评估框架（评估维度：规格弹性、高可用方案、备份恢复能力、扩展支持度、监控集成度）